document.addEventListener('DOMContentLoaded', init);

function validApp(){
   console.log("In the app.js")
}

function init() {    
    if(!validApp()){        
        return;
    }
    
    /* your code */
}